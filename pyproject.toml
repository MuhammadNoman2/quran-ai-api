[project]
name = "quran-ai-api"
version = "0.1.0"
description = "Local-first Quran recitation recognition and correction API"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
asyncio_mode = "auto"

[tool.ruff]
line-length = 100
target-version = "py310"
